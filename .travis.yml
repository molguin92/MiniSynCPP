language: c++
compiler:
  - clang
  - gcc

os: linux
dist: xenial

before_install:
  - mkdir ./cmake_build
  - cd ./cmake_build
  - cmake ..

script:
  - cmake --build . --target all -DCMAKE_BUILD_TYPE:STRING=DEBUG -- -j 4

# testing
after_success:
  - ./tests/minisyncpp
